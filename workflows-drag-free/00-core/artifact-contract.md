---
id: artifact-contract
kind: policy
status: active
---

# Artifact Contract

Durable workflow outputs must use the declared metadata root, deterministic names, and the repository's required indexes. An artifact must identify its scope, source evidence, status, unresolved items, and verification results. Do not overwrite unrelated records, duplicate an active artifact tree, or file an item as completed before its acceptance checks pass.
