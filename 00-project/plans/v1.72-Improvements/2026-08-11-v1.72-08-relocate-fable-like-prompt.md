# 2026-08-11 18:04

# Relocate or Remove the fable-like Prompt from the Planning Workflow Directory

**Status:** FINALIZED — consolidated and updated in place
**Priority:** P1  **Severity:** S2
**Finalisation source:** Original plan plus the dated consolidated review addendum below, validated against the live prompt and the `00-meta`/`00-docs` boundary READMEs.

## Summary, selected decision, and bounded scope

Remove `01-planning-and-organizing/fable-like.md` from the executable planning directory. The selected retention path is a clearly labelled reference snapshot under `00-Meta-Workflow/00-meta/`, because the live boundary says `00-meta` contains reusable workflow-system references/design documents while `00-docs` is for generated reports and archived reviews (`00-Meta-Workflow/00-meta/README.md:3-15,52-59`; `00-Meta-Workflow/00-docs/README.md:3-23`).

**Selected destination:** `00-Meta-Workflow/00-meta/fable-5-system-prompt-snapshot.md`.

**Retention precondition:** Do not copy or move the prompt until its provenance, applicable license or written permission, and repository retention authorization have been verified and recorded. Never infer permission from the file's presence. If the gate fails, do not retain a snapshot; remove the source from the executable planning directory only after the owner explicitly approves deletion and records the reason. The archive path is therefore the default only when the gate passes.

**In scope:** the source move/removal, a boundary header, the `00-meta/README.md` index/boundary wording, the planning README workflow-only boundary, and active-reference verification.

**Out of scope:** rewriting the captured prompt, changing its embedded historical wording, deciding a broader v2 taxonomy, modifying consumer projects, or silently rewriting historical research/plan records.

## Live evidence and assumptions

- The source exists and is 1,585 lines; it identifies itself as a Claude Fable 5 system prompt and contains product-specific behavior and environment assumptions (`01-planning-and-organizing/fable-like.md:1-30,78-80`; total file length verified before implementation).
- `01-planning-and-organizing/README.md` is an executable workflow index, while `00-meta/README.md` distinguishes workflow-system design/reference material from generated output (`00-meta/README.md:3-15,52-59`).
- `00-docs/README.md` currently defines its directory as generated reports and archived reviews (`00-docs/README.md:3-23`); it is not the selected destination for this product prompt snapshot.
- Existing historical mentions do not establish active instruction status; active workflow references must be searched separately from plans/research.

## Priority-ordered implementation roadmap

### Phase 1 — P1/S2: Provenance, permission, and collision preflight

**Objective:** Establish that retention is lawful/authorized and that the move is reversible before changing the tree.

**Tasks:**

1. Confirm the source exists; record its byte/line count and a pre-move checksum for later preservation verification (Small).
2. Verify and record the source provenance, applicable license or written permission to retain it in this repository, and owner authorization for the selected snapshot path. If any item is unavailable, stop the retention path and use the explicit deletion fallback only with owner approval (Medium).
3. Confirm `00-Meta-Workflow/00-meta/README.md` is the destination index, confirm the destination does not conflict, and confirm no active workflow depends on the old path (Small).

**Dependencies:** This gate precedes every move, copy, or deletion. Plan 07 must wait until Phase 2 has completed because it indexes the final planning directory; no v2 taxonomy decision is required.

**Validation and exit criteria:** Provenance/license/permission/authorization evidence is recorded, the collision check is clear, and the implementation has a selected pass (snapshot) or fail (approved deletion) decision before modifying files.

### Phase 2 — P1/S2: Move to a labelled `00-meta` snapshot or perform approved removal

**Objective:** Prevent the product prompt from being ingested as a planning instruction while preserving source material only when authorized.

**Snapshot path when Phase 1 passes:**

1. Use `git mv` to move the source to `00-Meta-Workflow/00-meta/fable-5-system-prompt-snapshot.md` (Small).
2. Prepend this exact boundary header and do not otherwise rewrite the captured source:

   > **Reference-only snapshot — not an agent instruction.** This file is a captured Claude Fable 5 system prompt for a product-specific `claude.ai` environment. Its paths, tools, policies, and runtime assumptions do not apply to Workflow-Scripts or consumer repositories.
   > **Source/provenance:** Record the verified original source and capture context before retention.
   > **License/permission:** Record the verified license or written permission and repository retention authorization before retention.
   > **Capture date:** Historical; preserve the internal stamps and source content below.
   > **Reason retained:** Reference-only historical material for model-behavior and multi-model orchestration research.

   (Small)
3. Replace the header fields with the verified evidence from Phase 1; do not leave invented, empty, or ambiguous claims (Small).
4. Add the snapshot to `00-meta/README.md` as a historical/reference-only snapshot, explicitly stating that it is not an active workflow instruction; retain the existing `00-meta` versus `00-docs` rule and do not misclassify the file as generated output (Small).

**Fallback when Phase 1 fails:** With explicit owner approval, delete the source from `01-planning-and-organizing/` without creating a snapshot; record the failed retention precondition and deletion authorization in the implementation's normal project log. Do not alter the source into an unverified “archive” (Small).

**Validation and exit criteria:** The planning directory contains no `fable-like.md`; a retained snapshot has the exact boundary header, verified metadata, and unchanged embedded source after the header; an approved deletion has no replacement snapshot.

### Phase 3 — P2/S3: Prevent recurrence and verify active references

**Objective:** Make the directory boundary explicit and distinguish stale historical mentions from active instructions.

**Tasks:**

1. Add a planning README boundary line: `01-planning-and-organizing/` contains executable planning workflows only; reference material belongs in `00-Meta-Workflow/00-meta/` or the relevant skill entry (Small).
2. Search active workflow files and README files for `fable-like` and the old path; separately search plans/research for historical mentions and record both results (Small).
3. Verify no active workflow instructs an agent to load, follow, or attach the old path; preserve historical mentions unless they are active instructions (Small).
4. Run `./scripts/validation/check-active-markdown-links.sh` and inspect all links changed by this plan, including the new `00-meta` index entry (Small).

**Dependencies:** Phase 2 must finish before Plan 07. The final planning README boundary line is part of Plan 07's indexed state if Plan 07 has not yet started; coordinate the shared edit rather than overwriting it.

**Validation and exit criteria:** Active-reference search and link validation pass, historical references are identified rather than silently rewritten, and Plan 07 can index the final directory without seeing the prompt as a workflow.

## Risks and mitigations

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Retention lacks lawful provenance or permission | Possible | Medium | Make evidence and owner authorization a hard precondition; use approved deletion fallback |
| Snapshot is mistaken for executable instructions | Possible | Medium | Use the exact reference-only header, `00-meta` index label, and planning-directory boundary |
| Historical stamps or source wording are altered | Possible | Medium | Record checksum/line count and compare the post-move body byte-for-byte after the header |
| Historical records mention the old path | Likely | Low | Search active files separately and preserve historical records unless they are active instructions |
| Plan 07 indexes a partially moved tree | Possible | Low | Hard-order Plan 08 before Plan 07 and verify the final tree first |

## Final acceptance and exit criteria

- [ ] The provenance, license/permission, and retention-authorization gate is recorded before any retention move.
- [ ] `fable-like.md` is absent from `01-planning-and-organizing/`.
- [ ] If authorized, the snapshot is exactly at `00-Meta-Workflow/00-meta/fable-5-system-prompt-snapshot.md` and begins with the reference-only/non-instruction header; otherwise the approved deletion path is recorded and no snapshot exists.
- [ ] The embedded source content and internal historical stamps are preserved byte-for-byte after the boundary header when a snapshot is retained.
- [ ] `00-meta/README.md` identifies the snapshot as historical/reference-only and preserves the directory boundary; `01-planning-and-organizing/README.md` states the workflow-only boundary.
- [ ] Active-workflow search finds no old-path instruction, and historical mentions are identified rather than silently rewritten.
- [ ] Active Markdown link validation passes.
- [ ] Plan 08 is complete before Plan 07 begins; no v2 taxonomy or consumer-project change is included.

## Consolidated Review Addendum

### 2026-08-11 18:04 — Plan Review (Model: gpt-5.6-terra)

#### P1 — S2/P1: Add a provenance, license, and permission gate

**Rationale:** The live file is a product-specific system prompt, but its provenance and retention rights are not established by repository presence (`fable-like.md:1-30`; `00-meta/README.md:52-59`). Retaining an unauthorized third-party prompt is a material compliance and governance risk. **Actionable fix:** Phase 1 makes verified provenance, license/written permission, and owner retention authorization a hard precondition, with an explicit approved-deletion fallback. **Disposition:** Accepted and integrated.

#### P1 — S2/P1: Place the retained artifact consistently with the live boundary

**Rationale:** `00-docs/README.md:7-23` reserves `00-docs` for generated reports and archived reviews, while `00-meta/README.md:3-15,52-59` owns workflow-system references/design material. The former destination would contradict the repository's stated boundary. **Actionable fix:** The selected destination is the clearly labelled `00-meta` snapshot, with an index entry and non-instruction header; no `00-docs` amendment is assumed. **Disposition:** Accepted and integrated.

#### P2 — S3/P2: Make the conditional retention branch implementation-ready

**Rationale:** A plan that only says “archive” cannot proceed safely when permission evidence is absent. **Actionable fix:** Phase 2 defines the pass (authorized snapshot) and fail (owner-approved removal without a snapshot) branches, including validation and rollback-preserving evidence. **Disposition:** Accepted and integrated.
