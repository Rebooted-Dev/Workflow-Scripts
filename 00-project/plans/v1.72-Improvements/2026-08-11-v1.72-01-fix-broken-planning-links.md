# 2026-08-11 18:03

# Fix Broken Cross-Reference Links in 01-planning-and-organizing

**Status:** FINALIZED — updated in place; no successor plan  
**Priority:** P1  **Severity:** S2
**Rationale:** Nine relative Markdown links in the active planning directory currently resolve to nonexistent files. The defect is a medium-impact, likely-path documentation failure with workarounds, so it is S2/P1 under `00-Meta-Workflow/00-meta/severity-priority-rubric.md`.
**Current evidence:** On 2026-08-11, link resolution found two broken links in `00-research-and-plan.md`, two in `01-plan-review.md`, three in `02-finalise-plan.md`, and two in `README.md` (nine total). The valid targets are under `../02-code-build/` and `../00-Meta-Workflow/`.

## Bounded scope and change inventory

Only these four workflow files may be edited by this plan:

- `01-planning-and-organizing/00-research-and-plan.md`
- `01-planning-and-organizing/01-plan-review.md`
- `01-planning-and-organizing/02-finalise-plan.md`
- `01-planning-and-organizing/README.md`

The implementation changes exactly nine hyperlinks:

- **Href plus visible text (4):** the execution link in `00-research-and-plan.md`, the execution link in `01-plan-review.md`, and the execution and confirmation links in `02-finalise-plan.md`; change `02-build-code`/`../../` to `02-code-build`/`../`.
- **Href only (5):** the rubric links in `00-research-and-plan.md`, `01-plan-review.md`, `02-finalise-plan.md`, and both rubric links in `README.md`; change the broken traversal to `../00-Meta-Workflow/00-meta/severity-priority-rubric.md` without changing visible text.
- **Out of scope:** source/workflow behavior, plain-text rubric references, other plans, the changelog, and creation of a link-checking tool or documentation entry.

## Priority-ordered implementation roadmap

### Phase 1 — P1/S2: Correct the nine hyperlinks

**Tasks:**

1. Update the two links in `00-research-and-plan.md` (Small; dependency: Plan 02's shared-file wording changes complete).
2. Update the two links in `01-plan-review.md` (Small; dependency: task 1 is complete if the same file is touched by the coordinated batch).
3. Update the three links in `02-finalise-plan.md` (Small; dependency: Plan 02's shared-file wording changes complete).
4. Update both rubric hrefs in `README.md` (Small; dependency: none beyond the target files being unchanged by another edit).
5. Inspect the diff and preserve every non-link character, including plain-text rubric references (Small; dependency: tasks 1–4).

**Coordination dependency:** Apply Plan 02 first. Complete Plan 01 before Plan 08 and Plan 07, because those plans edit nearby planning workflow/README surfaces. Do not run concurrent writers against these files.

**Validation and exit criteria:** Resolve every Markdown href in `01-planning-and-organizing/` against the source file's directory and confirm all targets exist; confirm exactly four visible-text changes, five href-only changes, and zero plain-text rubric changes; confirm no old linked `02-build-code` href/text remains.

## Risks and mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| Plan 02, Plan 08, or Plan 07 overlaps the same lines | Medium | Low | Use the hard sequence Plan 02 → Plan 01 → Plan 08 → Plan 07 and inspect the diff after each plan |
| A future directory rename recreates broken links | Possible | Medium | Defer a separate P3 checker proposal; do not enlarge this surgical link fix |

## Deferred follow-up (P3/S3)

A repeatable Markdown-link checker is deliberately **deferred**, not an implementation task or acceptance criterion here. If separately approved, its follow-up plan must specify the repository root, relative-link resolution rule, exclusions, exit code/zero-broken-link contract, and the indexed documentation destination before implementation. No checker, `00-project/docs/agents/markdown-link-checking.md`, changelog entry, or docs-index edit is created by this plan.

## Acceptance criteria

- [ ] All nine currently broken hrefs resolve to existing files after implementation.
- [ ] The four href-plus-visible-text corrections display the real `02-code-build`/`../` paths.
- [ ] The five href-only corrections change no visible text.
- [ ] Plain-text rubric references are unchanged.
- [ ] The final diff contains only the nine intended link corrections in the four named files.

## Inline Review Addenda

### 2026-08-11 17:51 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The original plan did not distinguish visible-text edits from href-only edits and did not name the Phase 2 documentation destination. **Evidence/rationale:** Nine hrefs are broken, but only four display the old `02-build-code` path; changing plain-text rubric spans would create unrelated churn. **Actionable correction:** The inventory and validation above enumerate 4 + 5 changes. **Disposition:** Accepted and integrated.

### 2026-08-11 18:03 — Consolidated Plan Review (Model: gpt-5.6-terra)

#### P0

No additional findings.

#### P1

No additional findings.

#### P2

No additional findings.

#### P3

1. **Severity:** S3  **Priority:** P3
   **Rationale:** The proposed repeatability guardrail named a documentation file that is absent in the current tree and did not define a checker contract or docs-index inclusion. The link defect itself is already bounded and independently verifiable, so this guardrail is not a blocker.
   **Actionable fix:** Defer the checker. If it is later reintroduced, require the contract and indexed documentation specified in the deferred-follow-up section; keep it out of this plan's exit criteria.
   **Evidence:** `00-project/docs/agents/markdown-link-checking.md` does not currently exist; `00-project/docs/README.md` is the current documentation index.
   **Disposition:** Accepted and integrated.

**Validation performed:** Read the two planning workflow specifications, the four target workflow files, the shared rubric, and the current link targets; independently resolved all Markdown links in the four target files and confirmed the pre-change count of nine broken hrefs. No additional findings beyond the P3 deferral were identified.
