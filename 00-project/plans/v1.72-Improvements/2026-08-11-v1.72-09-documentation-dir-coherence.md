# 2026-08-11 18:04

# 04-documentation Coherence: Deduplicate Repeated Content and Correct README Semantics

**Status:** FINALIZED — consolidated and updated in place
**Priority:** P2  **Severity:** S3
**Finalisation source:** Original plan plus the dated consolidated review addendum below, validated against the live documentation workflows and README.

## Summary and bounded scope

Make `04-documentation/09-optional.md` the optional-docs checklist authority and `04-documentation/ascii-art-prompts.md` the ASCII-art reference authority, while preserving unique content and correcting the documentation hub's semantics. This is a documentation-only consolidation; it does not alter workflow behavior or the terminal-gate workflow.

**In scope:** `04-documentation/01-create-docs.md`, `02-sync-documentation.md`, `00-doc-templates.md`, `09-optional.md`, `ascii-art-prompts.md`, and `04-documentation/README.md`.

**Out of scope:** `04-documentation/03-mark-completed.md`, application code, a new documentation taxonomy, removal of the unique `Need a diagram?` selection flow, and unrelated documentation content.

## Live evidence and required invariants

- Optional-docs content is duplicated in `01-create-docs.md:57-90` and `09-optional.md:1-30,32-107`; `01-create-docs.md` itself says to recommend only the smallest set (`:67-89`).
- ASCII-art guidance is duplicated in `00-doc-templates.md:498-562`, `01-create-docs.md:354-372`, `02-sync-documentation.md:71-74`, and `04-documentation/README.md:85-94`; `ascii-art-prompts.md` is the intended authority.
- The `Need a diagram?` selection flow in `00-doc-templates.md:502-526` is a unique reusable decision flow, not a duplicate block to delete.
- The README currently has one priority table (`04-documentation/README.md:35-42`) even though create and sync workflows define different P0-P3 meanings (`01-create-docs.md:47-55`; `02-sync-documentation.md:22-30`).
- The README diagram currently makes `09-optional` and `ascii-art-prompts` children of `03-mark-completed` (`04-documentation/README.md:46-72`), despite 03 being described as a terminal gate (`:96-104`).
- The ASCII alignment checklist is live at `ascii-art-prompts.md:324-333` and is the verification standard for the revised diagram.

## Priority-ordered implementation roadmap

### Phase 1 — P2/S3: Consolidate the optional-docs checklist

**Objective:** Keep optional/conditional add-on guidance in one authority without losing create-specific priority guidance or output examples.

**Tasks:**

1. Replace the enumerated optional/conditional checklist in `01-create-docs.md:67-90` with a 2–3 line pointer to `./09-optional.md`, retaining the instruction to choose the smallest high-leverage set (Small).
2. Preserve the create workflow's own priority bucket definitions and placement notes at `01-create-docs.md:47-60`; remove only duplicated optional add-on enumeration (Small).
3. Preserve the output structure/tree illustration in the later create-docs section; remove checklist material from it only if it duplicates `09-optional.md` (Small).
4. Replace the duplicated preamble in `09-optional.md` with this exact merged header:

   > **Use this checklist when creating or reviewing documentation.** Select only optional additions that remove real confusion or risk; default to the smallest high-leverage set, usually 1–3 items. Apply it after (or during) the core documentation work, and skip items that do not fit the project.

   (Small)

**Dependencies:** None outside this phase; retain `09-optional.md` as the single checklist authority.

**Validation and exit criteria:** Optional add-on/nice-to-have checklist entries occur only in `09-optional.md`; `01-create-docs.md` points to it and retains its create-specific priorities; the exact merged header appears once.

### Phase 2 — P2/S3: Consolidate ASCII-art references while preserving the unique selection flow

**Objective:** Centralize reusable ASCII-art prompts, references, and quality guidance without deleting the template's unique decision flow.

**Tasks:**

1. In `00-doc-templates.md`, preserve the complete `## ASCII Art Diagram Selection Flow` heading and `Need a diagram?` diagram (`:502-526`), including its plain-text decision and both outcomes; do not replace this unique flow with a pointer (Small).
2. Replace only the duplicated character reference, generic best-practice, and explanatory material adjacent to that flow in `00-doc-templates.md` with a concise pointer to `./ascii-art-prompts.md` (Small).
3. Replace duplicate ASCII-art reference blocks with one-line pointers in all four named consumers: `01-create-docs.md`, `02-sync-documentation.md`, `00-doc-templates.md`, and `04-documentation/README.md` (Small).
4. Delete the second standalone intro sentence in `ascii-art-prompts.md` using the stable content anchor immediately before `## Architecture & System Diagrams`; retain the first Purpose intro (`:3-4`) (Small).
5. Remove the hardcoded `**Last Updated**: 2026-01-21` footer from `ascii-art-prompts.md` and do not replace it with an ambiguous date (Small).

**Dependencies:** Phase 1 is independent; this phase must preserve the selection flow before any duplicate-block deletion.

**Validation and exit criteria:** The authority retains the reusable ASCII prompts/reference/checklist; all four consumers have at most concise pointers plus their own unique instructions; the `Need a diagram?` flow remains intact; exactly one intro remains; the stale date is absent.

### Phase 3 — P2/S3: Correct create-versus-sync priority semantics

**Objective:** Make every priority bucket in the README match the corresponding workflow instead of implying a single shared meaning.

**Tasks:**

1. Replace the single ambiguous priority table with explicitly labelled create and sync semantics. Preserve all buckets:
   - **Create P0:** critical user-facing docs; **P1:** core technical docs; **P2:** detailed technical docs; **P3:** enhanced docs.
   - **Sync P0:** incorrect docs causing wrong usage, broken setup, or unsafe behavior; **P1:** missing docs for critical flows; **P2:** reorganization, consolidation, cross-links, and reference completeness; **P3:** nice-to-have diagrams, polish, and deep examples.
   (Small)
2. Keep examples beneath the bucket that owns them and do not imply that create and sync use the same P0 meaning (Small).

**Dependencies:** Validate against `01-create-docs.md:47-55` and `02-sync-documentation.md:22-30` before editing the README.

**Validation and exit criteria:** The README names both create and sync tables (or an equivalently explicit combined table), and every P0/P1/P2/P3 description and example is attributable to the correct workflow.

### Phase 4 — P2/S3: Correct the documentation hub diagram without editing 03

**Objective:** Show shared references and the conditional completion gate accurately.

**Tasks:**

1. Edit only `04-documentation/README.md` for the hub diagram. Show `00-doc-templates`, `09-optional`, and `ascii-art-prompts` as shared references feeding `01-create-docs` and `02-sync-documentation`; they must not appear as children of 03 (Small).
2. Show `03-mark-completed` as a **conditional** terminal gate after create/sync: after documentation work, branch on “Need completion verification/archive?”; only the yes branch enters `03-mark-completed`, while the no branch ends without that gate (Small).
3. Do not edit `03-mark-completed.md`; it remains the sole terminal authority when the conditional gate is selected (Small).
4. Apply `ascii-art-prompts.md:324-333`: check box widths, branch spacing, line joins, consistent arrows, vertical spacing, centering, and loop-free paths (Small).

**Dependencies:** Independent of Plans 01–08. Plan 06 may edit 03 separately, but this plan must not touch that file; Phase 4's diagram must remain valid against its terminal-gate description.

**Validation and exit criteria:** The README diagram has shared-reference relationships, a clearly labelled optional/conditional 03 gate, no 09/ascii child relationship to 03, and passes the alignment checklist; `03-mark-completed.md` has no diff.

## Risks and mitigations

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Removing a duplicate also removes unique selection guidance | Possible | Medium | Preserve the complete `Need a diagram?` flow before deduplication and compare its semantic lines |
| Create and sync priorities are conflated again | Possible | Medium | Use two explicitly labelled P0-P3 sets and validate against both live workflows |
| A diagram implies every docs run must pass through 03 | Possible | Medium | Draw an explicit decision branch; label 03 as conditional terminal verification/archive |
| Pointers make create-docs feel less self-contained | Possible | Low | Keep one-line pointers adjacent to removed blocks and preserve unique local flow/content |
| ASCII diagram misaligns | Possible | Low | Apply the named alignment checklist and inspect rendered monospace text |

## Final acceptance and exit criteria

- [ ] Optional checklist content exists only in `09-optional.md`; its header matches the exact merged content.
- [ ] The unique `Need a diagram?` selection flow remains in `00-doc-templates.md` unchanged in meaning.
- [ ] Reusable ASCII-art prompts/reference/checklist content is authoritative in `ascii-art-prompts.md`; all four named consumers have bounded pointers or unique local content only.
- [ ] The duplicate ASCII intro is removed at the stated content anchor, leaving one Purpose intro, and the stale date is absent.
- [ ] README priority guidance explicitly preserves every create and sync P0-P3 distinction.
- [ ] README hub diagram shows shared references feeding 01/02 and a conditional 03 terminal gate; 09 and ascii are not 03 children.
- [ ] Only the README receives the hub-diagram edit; `03-mark-completed.md` is unchanged.
- [ ] The revised diagram passes the alignment checklist and no unrelated files are changed.

## Consolidated Review Addendum

### 2026-08-11 18:04 — Plan Review (Model: gpt-5.6-terra)

#### P1 — S3/P1: Preserve the unique `Need a diagram?` flow

**Rationale:** `00-doc-templates.md:502-526` contains a selection flow that is not merely a copied character/reference block. Replacing the entire section with a pointer would remove a useful decision aid. **Actionable fix:** Phase 2 explicitly preserves the heading, diagram, and semantics, while deduplicating only adjacent reusable guidance. **Disposition:** Accepted and integrated.

#### P1 — S3/P1: Preserve every create-versus-sync priority difference

**Rationale:** The create workflow defines P0 critical user-facing docs through P3 enhanced docs (`01-create-docs.md:47-55`), while sync defines P0 incorrect/unsafe usage through P3 diagrams/polish (`02-sync-documentation.md:22-30`). A single generic table would continue to mislead. **Actionable fix:** Phase 3 lists all four buckets for both workflows and requires examples to remain under their owning semantics. **Disposition:** Accepted and integrated.

#### P1 — S3/P1: Make the completion workflow conditional in the diagram

**Rationale:** The live README describes 03 as a terminal gate (`04-documentation/README.md:96-104`), but its current diagram makes it look mandatory and places 09/ascii beneath it (`:46-72`). **Actionable fix:** Phase 4 requires an explicit “Need completion verification/archive?” decision, with only the yes branch entering 03 and a no branch ending. **Disposition:** Accepted and integrated.
