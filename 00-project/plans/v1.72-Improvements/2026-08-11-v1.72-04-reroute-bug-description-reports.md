# 2026-08-11 18:03

# Re-route Bug Description Reports and Align Naming + Changelog Discovery

**Status:** FINALIZED — implementation-ready and updated in place
**Priority:** P1  **Severity:** S2
**Rationale:** `03-debugging/01-bug-description.md` treats an investigation/findings report as an active plan, stores it in a plan location, and uses non-host-aware artifact lookups. This pollutes active-plan discovery and weakens historical traceability.
**Exact target:** `03-debugging/01-bug-description.md` only.

## Scope boundary

Change only report routing, metadata-root/host-policy artifact discovery, naming, and the explicitly selected troubleshooting cross-link in the target workflow. Do not edit `03-debugging/02-bug-fix-workflow.md`, `04-documentation/03-mark-completed.md`, README routing semantics, or existing report files. Existing reports stay where they are; this plan does not perform a bulk migration.

## Confirmed live evidence

- `03-debugging/01-bug-description.md:28-33` looks up bare `troubleshooting/` and prefers `docs/CHANGELOG.md`/`CHANGELOG.md`; `:45-56` and `:92-97` also refer to bare historical artifact locations.
- `03-debugging/01-bug-description.md:184-208` routes new reports to `plans/` and uses a non-standard long-date filename.
- `00-Meta-Workflow/00-meta/naming-conventions.md:44-59` defines metadata-root resolution, research-report storage, active-plan storage, completed-plan storage, and changelog/troubleshooting paths.
- `04-documentation/03-mark-completed.md:20-25` and `:89-95` use active-plan discovery and host-policy archive routing; the live file contains no bug-report special case. No downstream edit is required for that point.
- `03-debugging/02-bug-fix-workflow.md:103-116` confirms directory-based changelog/troubleshooting conventions and is the cross-workflow wording reference.

## Priority-ordered implementation roadmap

### Phase 1 — P1/S2: Make every artifact lookup metadata-root and host-policy aware

**Tasks:**

1. Replace every bare `troubleshooting/`, `plans-completed/`, changelog, and other metadata artifact lookup in Inputs, Step 1, Step 2, Step 4, and related requirements with a resolved `<metadata-root>` path. Resolve the root using `naming-conventions.md`; when a host documents a different completed-plan or log location, follow that host policy rather than inventing a directory (Small).
2. In Step 1 Agent 1, read `<metadata-root>/troubleshooting/index.md` and relevant `<metadata-root>/troubleshooting/<category>/` entries first (Small).
3. In Step 1 Agent 2, read `<metadata-root>/changelog/index.md` and recent `<metadata-root>/changelog/<type>/` entries first, then review the host-policy completed-plan location as a separate historical source. Use `docs/CHANGELOG.md` or `CHANGELOG.md` only when the host is documented as single-file (Small).

**Dependencies:** `naming-conventions.md` is the path authority. Coordinate with Plans 02 and 03 if they change adjacent routing or debugging lifecycle wording; no code dependency exists.

**Risks and mitigations:** A host may lack a directory layout (Medium/Low); resolve the host metadata policy and use the documented single-file fallback, never create an ad hoc root-level directory. A broad text replacement may alter non-artifact links (Low/Low); review every changed lookup in context.

**Validation and exit criteria:** Every artifact lookup in the target names `<metadata-root>` or an explicitly documented host-policy location; directory-first versus single-file fallback is unambiguous; all affected lookup sites have been reviewed without changing unrelated workflow links.

### Phase 2 — P1/S2: Route new reports to research with the standard name

**Tasks:**

1. Rewrite Step 5 to save new reports to `<metadata-root>/research/bug-description-YYMMDD-HHMM-{model}.md`, citing `../00-Meta-Workflow/00-meta/naming-conventions.md` as the naming and location authority (Small).
2. Update Output Requirements and Acceptance Criteria to use exactly the same research location and filename; remove all new-report instructions that save to `plans/` (Small).
3. Retain the self-contained report requirements and add the selected traceability extension: when a related troubleshooting entry exists, link to the report from that entry and add the reciprocal link from the report; if the entry is not writable, record the reason rather than silently omitting the backlink (Small).

**Dependencies:** Phase 1 must establish the path vocabulary before the report-storage text is edited.

**Risks and mitigations:** A consumer may link to the old filename (Low/Low); preserve old files and document the no-migration decision in Phase 3.

**Validation and exit criteria:** Step 5, Output Requirements, Acceptance Criteria, and any report example all agree on research routing and the standard filename; no new report is directed to `plans/`.

### Phase 3 — P2/S3: Preserve historical reports and record downstream verification

**Tasks:**

1. Select and record the migration policy: leave existing `plans/bug-description-*.md` files in place as historical records; perform no bulk migration. A later, separately approved migration must inventory and repair every inbound cross-link before moving anything (Small).
2. Record the live downstream result explicitly: `04-documentation/03-mark-completed.md` has no bug-report special case, so this plan makes no edit there and does not claim that one exists (Small).
3. Verify the final target for stale bare artifact lookups and contradictory report-storage examples; classify the documentation change under changelog category `changed/` without creating that entry in this plan (Small).

**Dependencies:** Phases 1 and 2 must be complete. The host changelog policy determines where a later implementation records the `changed/` entry.

**Risks and mitigations:** Historical files may be mistaken for active reports (Low/Medium); leave them untouched and rely on the host's active-plan discovery policy plus explicit new research routing.

**Validation and exit criteria:** The no-bulk-migration decision is recorded, the downstream check is a concrete “no special case found” result, and a final scan finds no stale new-report routing or unsupported artifact lookup.

## Final acceptance criteria

- [ ] All artifact lookups in the target resolve through `<metadata-root>` and the host's documented policy, with the directory-first changelog hierarchy and documented single-file fallback.
- [ ] New reports resolve to `<metadata-root>/research/bug-description-YYMMDD-HHMM-{model}.md`.
- [ ] No new-report instruction saves to `plans/`; existing reports remain in place and are not bulk-migrated.
- [ ] The troubleshooting cross-link is labeled as a deliberate extension and is reciprocal when an entry exists.
- [ ] The live `03-mark-completed.md` result is recorded accurately: no bug-report special case exists and no edit is planned there.
- [ ] Changelog category is `changed/`; no changelog or other file is changed as part of this plan update.

## Prior inline review addendum

### 2026-08-11 17:51 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The original plan left the cross-link rationale, changelog hierarchy, downstream verification, and legacy-report migration as choices. **Evidence/rationale:** Existing reports can have inbound links, and artifact conventions differ by host. **Actionable correction:** The plan labels the troubleshooting cross-link as a deliberate extension, defines directory-first/single-file fallback precisely, records a concrete downstream result, and defaults to preserving historical reports. **Disposition:** Accepted and integrated.

## Consolidated review addendum

### 2026-08-11 18:03 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The plan still needed to require metadata-root and host-policy resolution at every artifact lookup, not only at report storage and changelog discovery. **Evidence/rationale:** The live target uses bare `troubleshooting/` and `plans-completed/` references at `:28-33`, `:45-56`, and `:92-97`; `naming-conventions.md:44-59` is the applicable authority. **Actionable fix:** Phase 1 now inventories and rewrites all metadata artifact lookups and requires host-policy handling for completed plans and logs.
2. **S2/P1 — Finding:** The migration decision needed to be an explicit default rather than an optional conditional. **Evidence/rationale:** Existing `plans/bug-description-*.md` records may have inbound links. **Actionable fix:** Phase 3 selects leave-existing-reports-in-place and prohibits bulk migration.
3. **S3/P2 — Finding:** A prior formulation falsely implied that `03-mark-completed.md` special-cases bug reports. **Evidence/rationale:** The live file's active discovery is generic at `:89-95` and contains no bug-report exception. **Actionable fix:** The plan records “no special case found” and requires no edit to that file.
4. **No additional findings.** The primary scope is bounded to the named target and has explicit dependencies, risks, effort labels, validation, and exit criteria.
