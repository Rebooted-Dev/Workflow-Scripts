# 2026-08-11 18:03

# Generalize the Example-Contaminated Agent Roles in 03-mark-completed.md Phase 2

**Status:** FINALIZED — implementation-ready and updated in place
**Priority:** P1  **Severity:** S2
**Rationale:** `04-documentation/03-mark-completed.md` presents a product-specific file tree as generic verification guidance. Agents can inspect nonexistent files and miss the files named by the actual plan. The workflow remains usable, so this is S2/P1.
**Exact target:** `04-documentation/03-mark-completed.md` only.

## Scope boundary

Generalize Phase 2's verification roles, remove consumer-specific text throughout the target's role and flagged-issues example material, add adaptable task coverage, and link the shared spawning policy. Preserve the existing per-agent output fields and flagged-issue output format. Do not change archive behavior, completion-mark semantics, or unrelated documentation.

## Confirmed live evidence

- `04-documentation/03-mark-completed.md:98-115` defines Phase 2 role instructions and the required per-agent output fields.
- `04-documentation/03-mark-completed.md:101-108` contains consumer-specific paths and identifiers in Agents A–D, while E–F are domain-oriented.
- `04-documentation/03-mark-completed.md:145-165` contains a flagged-issues example with a concrete consumer path and no illustrative-only warning.
- `00-Meta-Workflow/00-meta/agent-spawning-policy.md:3-10` defines evidence-based role selection and the shared total-session cap; `:26` requires primary-reviewer verification.

## Priority-ordered implementation roadmap

### Phase 1 — P1/S2: Replace fixed consumer roles with domain-first adaptable roles

**Tasks:**

1. Rewrite Phase 2 Agents A–F by verification domain first: critical path/data integrity, security/platform, bug/state/concurrency, UX/integration, lower-priority maintainability/performance, and documentation/log reconciliation. Each role must adapt its files, tasks, and line ranges from the named plan under verification; no product tree or hard-coded path is assumed (Small).
2. Add a direct link to `../00-Meta-Workflow/00-meta/agent-spawning-policy.md` beside the role block. State that role allocation is bounded by evidence and follows that shared policy; do not add a local numeric cap (Small).
3. Preserve the existing per-agent output fields exactly: Task ID, Claim, Files read, Verified?, Evidence, and Flag (Small).
4. Require a task-to-agent coverage matrix for every selected plan. The matrix must map each plan task/sub-task to its responsible verification domain/agent, the plan-derived files and line ranges, and a verification result/evidence or flag; no in-scope task may be left unmapped (Medium).
5. Add this Notes sentence: “Do not spawn unbounded agents; assign only bounded, plan-derived tasks and follow the [shared agent-spawning policy](../../../00-Meta-Workflow/00-meta/agent-spawning-policy.md) and its total-session cap.” (Small).

**Dependencies:** The shared policy must remain available. Plans 04 and 05 may update related debugging guidance but must not be treated as dependencies; Plan 09 must not touch this target.

**Risks and mitigations:** Generic roles may become vague (Medium/Low); require plan-derived file/line evidence and the coverage matrix. Domain overlap may cause duplicate work (Low/Low); assign one responsible agent per task in the matrix and identify secondary reviewers only when evidence requires them.

**Validation and exit criteria:** A scan of Phase 2 finds no consumer-specific filename, hook, component, framework identifier, or assumed product tree; every role is domain-first and plan-adaptable; the policy link is present; the matrix covers every task/sub-task; output fields are unchanged.

### Phase 2 — P1/S2: Delete consumer-specific examples while retaining usable generic shape

**Tasks:**

1. Delete all consumer-specific role and example text from Phase 2 and the Phase 5 flagged-issues sample, including concrete paths and identifiers. Replace the sample with one short fenced generic example using placeholders such as `src/entrypoint.ext` and `Task-1` (Small).
2. Put the exact lead-in `Illustrative example only (hypothetical consumer project; do not treat paths or findings as required):` immediately above the Phase 5 sample (Small).
3. Use the exact role-output fields in the generic example without changing their names or order; read the whole target for stale references to removed roles (Small).

**Dependencies:** Phase 1 defines the adaptable roles and coverage-matrix contract before examples are rewritten.

**Risks and mitigations:** A placeholder may still be mistaken for a required path (Low/Medium); retain the exact warning lead-in and use placeholders only. Deleting too much may remove required output structure (Low/Medium); compare the output block before and after.

**Validation and exit criteria:** The entire target contains no consumer-specific text; the retained example is fenced, generic, visibly illustrative, and contains no product path or identifier; role and output sections agree.

### Phase 3 — P2/S3: Whole-file consistency review

**Tasks:**

1. Search the whole target for stale role names, consumer-specific identifiers, unlabeled concrete paths, and conflicting spawning instructions; correct only those findings (Small).
2. Confirm the task-to-agent coverage matrix requirement and the unchanged completion/archive/output semantics remain present (Small).
3. Classify the documentation change under changelog category `changed/`; do not create or edit a changelog file in this plan update (Small).

**Dependencies:** Phases 1 and 2 must be complete.

**Risks and mitigations:** Whole-file cleanup could expand scope (Low/Low); limit edits to stale contamination or direct contradictions and preserve unrelated workflow behavior.

**Validation and exit criteria:** Whole-file stale-reference scan passes; output format and completion/archive semantics are unchanged; only the named target requires implementation changes.

## Final acceptance criteria

- [ ] Phase 2 role definitions are domain-first, adaptable to the selected plan, and contain zero consumer-specific identifiers or paths.
- [ ] The role block links to the shared policy and contains no local numeric cap.
- [ ] A task-to-agent coverage matrix is required and maps every task/sub-task to an agent/domain, plan-derived files/line ranges, and evidence or a flag.
- [ ] All consumer-specific text is deleted from the role and example material; the retained example is generic, fenced, and explicitly illustrative.
- [ ] The Phase 5 sample has the exact illustrative-only lead-in.
- [ ] Per-agent output fields and completion/archive/output semantics remain unchanged; whole-file stale-reference review passes.
- [ ] Notes includes an explicit unbounded-spawning prohibition linked to `../00-Meta-Workflow/00-meta/agent-spawning-policy.md`.
- [ ] Changelog category is `changed/`; no changelog file is changed as part of this plan update.

## Prior inline review addendum

### 2026-08-11 17:51 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The original plan made Phase 1 partly dependent on an unplanned policy decision and conflated handling concrete material with labeling the Phase 5 sample. **Evidence/rationale:** Generic roles need their policy boundary to be complete, while the Phase 5 sample is a separate deliverable. **Actionable correction:** Phase 1 folds in the policy citation, Phase 2 handles material with a selected generic-example default, and Phase 3 separately labels the Phase 5 sample. **Disposition:** Accepted and integrated.

## Consolidated review addendum

### 2026-08-11 18:03 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The plan needed a task-to-agent coverage contract rather than only generalized role descriptions. **Evidence/rationale:** The live Phase 2 output at `:98-115` verifies role results but does not require proving that every plan task was assigned. **Actionable fix:** Phase 1 now requires a matrix mapping every task/sub-task to a domain/agent, plan-derived files and line ranges, and evidence or a flag.
2. **S2/P1 — Finding:** The roles needed to be domain-first and adaptable, not merely renamed versions of consumer-specific roles. **Evidence/rationale:** The live target hard-codes product paths at `:101-108`; the shared policy supports evidence-triggered roles at `agent-spawning-policy.md:5-10`. **Actionable fix:** The roadmap selects verification domains first and derives files and responsibilities from the selected plan.
3. **S2/P1 — Finding:** The target needed an explicit guard against unbounded spawning and a policy link in Notes. **Evidence/rationale:** A role list without a bounded-spawning reminder can be expanded without evidence. **Actionable fix:** Final acceptance now requires a Notes sentence prohibiting unbounded spawning and linking the shared policy.
4. **S2/P1 — Finding:** Consumer-specific text must be removed rather than merely relabeled. **Evidence/rationale:** The live Phase 5 sample at `:145-165` contains a concrete consumer path. **Actionable fix:** Phase 2 requires deletion of consumer-specific role and sample text, retaining only a clearly labeled placeholder example.
5. **No additional findings.** The primary scope is bounded and includes dependencies, risks, effort labels, validation, and exit criteria.
