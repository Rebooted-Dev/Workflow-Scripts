# 2026-08-11 18:03

# Reconcile Agent-Spawning Guidance in 02-bug-fix-workflow.md with the Shared Policy

**Status:** FINALIZED — implementation-ready and updated in place
**Priority:** P1  **Severity:** S2
**Rationale:** `03-debugging/02-bug-fix-workflow.md` combines core roles with local “additional agents” maxima, creating a likely conflict with the shared agent-spawning policy. The impact is coordination failure rather than application breakage, so this remains S2/P1.
**Exact targets:** `03-debugging/02-bug-fix-workflow.md` and the explicitly linked Step 1 citation in `03-debugging/01-bug-description.md`.

## Scope boundary

Remove local agent-count guidance and duplicate batch-read wording only. Keep every evidence-trigger threshold, specialist condition, core responsibility, output requirement, and verification command unchanged unless a task below explicitly names it. Do not change the shared policy or unrelated workflows.

## Confirmed live evidence

- `03-debugging/02-bug-fix-workflow.md:30-48` and `:66-83` contain role lists, evidence triggers, and local “Maximum recommended” wording.
- `03-debugging/02-bug-fix-workflow.md:94-101` links to `../00-Meta-Workflow/00-meta/agent-spawning-policy.md`; the live policy at `:5-10` is the numeric authority.
- `03-debugging/01-bug-description.md:42-76` has five investigation roles but no policy link.
- `00-Meta-Workflow/00-meta/agent-spawning-policy.md:3-10` defines the shared cap and evidence-based role selection; its `:26` requires primary-reviewer verification.

## Priority-ordered implementation roadmap

### Phase 1 — P1/S2: Link to the shared policy without repeating its numeric cap

**Tasks:**

1. In `02-bug-fix-workflow.md` Step 2, remove `Maximum recommended` and the local additional-agent maximum. Add a direct link to `../00-Meta-Workflow/00-meta/agent-spawning-policy.md` and defer all agent-count decisions to that policy (Small).
2. Apply the same link-only replacement in Step 5 (Small).
3. In Step 6, retain a direct policy link but remove any duplicate numeric wording; the link, not the workflow, is the authority for the shared cap (Small).
4. Preserve the complete Step 2, Step 5, and Step 6 evidence-trigger lists and specialist conditions exactly, including their existing thresholds (Small).

**Dependencies:** The shared policy must remain available. Coordinate with Plans 03 and 04 before editing adjacent debugging workflow text; no application-code dependency exists.

**Risks and mitigations:** Readers may interpret removal of maxima as permission for unbounded spawning (Medium/Low); add policy links at every spawning section and retain evidence triggers. A mechanical edit may drop a trigger (Medium/Low); perform a pre/post bullet comparison.

**Validation and exit criteria:** Steps 2 and 5 contain no local numeric agent-cap rule or “Maximum recommended” line; Step 6 contains no competing numeric rule; each section links to the shared policy; all evidence-trigger bullets and thresholds are unchanged.

### Phase 2 — P1/S2: Add shared-policy routing to bug-description investigation

**Tasks:**

1. Immediately after the five-agent list in `01-bug-description.md` Step 1, add a direct link to `../00-Meta-Workflow/00-meta/agent-spawning-policy.md` stating that the list is subject to the shared total-session cap and evidence-triggered role rules, without restating the cap numerically (Small).
2. Verify Agent 1–5 responsibilities and the batch-read instruction remain unchanged (Small).

**Dependencies:** Phase 1's policy-link wording is the pattern for this citation; Plan 04 may edit the same file's artifact lookup text, so reconcile both edits before validation.

**Risks and mitigations:** The citation could be mistaken for a new local rule (Low/Low); use link-only wording and explicitly identify the shared policy as authoritative.

**Validation and exit criteria:** The Step 1 citation appears directly after the agent list, contains no local numeric cap, and the five responsibilities are unchanged.

### Phase 3 — P2/S3: Make batch-read guidance canonical

**Tasks:**

1. Remove the repeated batch-read sentence from `02-bug-fix-workflow.md` Step 2 and Step 5, without removing the requirement to batch-read when useful (Small).
2. Keep exactly one Notes sentence in the target: “Batch-read related files when useful; follow `../00-Meta-Workflow/00-meta/agent-spawning-policy.md` and have the primary reviewer verify findings.” (Small).
3. Do not duplicate this cleanup in `01-bug-description.md`; its existing sentence is outside this plan's selected cleanup target (Small).

**Dependencies:** Phase 1 must establish the policy link before the canonical Notes sentence is finalized.

**Risks and mitigations:** Removing duplicate text may reduce discoverability (Low/Low); retain the single Notes instruction and direct policy link.

**Validation and exit criteria:** The target has exactly one canonical batch-read Notes sentence, no stale local cap language, and no unrelated wording changes.

## Final acceptance criteria

- [ ] No “3-5 additional agents,” “Maximum recommended,” or other local numeric agent-cap rule remains in `02-bug-fix-workflow.md`.
- [ ] Every target spawning section links to the shared policy and does not repeat its numeric cap.
- [ ] `01-bug-description.md` Step 1 contains the policy citation immediately after its agent list and has no local numeric cap.
- [ ] All evidence-trigger thresholds and specialist conditions are unchanged.
- [ ] `02-bug-fix-workflow.md` has exactly one canonical batch-read Notes sentence.
- [ ] Changelog category is `changed/` and records the P1/S2 reclassification; no changelog file is changed as part of this plan update.

## Prior inline review addendum

### 2026-08-11 17:51 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The original severity/priority understated a likely violation of the shared cap, and its Phase 2 did not specify the actual Step 1 citation work. **Evidence/rationale:** The local maxima can conflict with the shared policy, while the five-agent bug-description list lacked a policy link. **Actionable correction:** Reclassified to S2/P1, made the Step 1 policy citation concrete, and selected a concrete P2 batch-read cleanup. **Disposition:** Accepted and integrated.

## Consolidated review addendum

### 2026-08-11 18:03 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P1 — Finding:** The plan needed to enforce link-not-repeat behavior for the shared numeric cap, including the existing Step 6 citation. **Evidence/rationale:** The live Step 6 text at `:100` currently repeats numeric policy guidance, while the policy at `agent-spawning-policy.md:5-10` is the sole numeric authority. **Actionable fix:** Phase 1 now requires direct policy links with no local numeric cap language in Steps 2, 5, or 6.
2. **S2/P1 — Finding:** The exit condition needed to distinguish removal of local numeric rules from preservation of evidence thresholds. **Evidence/rationale:** The live trigger lists at `:40-45`, `:75-80`, and `:94-98` are operational safeguards, not cap rules. **Actionable fix:** Each phase and the final acceptance criteria now require an unchanged threshold/bullet comparison while requiring no local numeric agent-cap rule.
3. **No additional findings.** Scope, dependencies, risks, effort labels, validation, and exit criteria are explicit and bounded.
