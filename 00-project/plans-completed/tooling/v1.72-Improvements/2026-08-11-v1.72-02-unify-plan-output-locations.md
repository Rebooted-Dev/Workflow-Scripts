# 2026-08-11 18:03

# Unify Plan and Research Output Locations Across Planning Workflows

**Status:** FINALIZED — updated in place; no successor plan  
**Priority:** P1  **Severity:** S2
**Rationale:** The planning workflows currently route artifacts to inconsistent locations. This is a medium-impact, likely-path failure with a human workaround, therefore S2/P1 under the shared rubric. P0/S2 is not supported by the current evidence; reclassify to P0 only if high-impact evidence (S0/S1: security, data loss, or outage) is later established.
**Authoritative policy:** `00-Meta-Workflow/00-meta/naming-conventions.md`, especially Metadata Root Resolution and Storage Location.

## Confirmed evidence and bounded scope

- `01-planning-and-organizing/00-research-and-plan.md` currently mixes `project/build/`, bare `plans/`, and an inline filename while omitting a research destination.
- `01-plan-review.md` currently names both `project/build/` and `project/plans/`.
- `02-finalise-plan.md:65` currently mandates root `plans/` and “project root”.
- The policy currently defines `<metadata-root>/research/` and `<metadata-root>/plans/`, but its resolution rule does not explicitly honor a host's root-level policy.
- The current Workflow-Scripts host confirms `00-project/` as its metadata root in `00-project/AGENTS.md` and `00-project/plans/README.md`; this does not justify imposing that layout on other hosts.

Only these four policy/workflow files may be edited by this plan:

1. `01-planning-and-organizing/00-research-and-plan.md`
2. `01-planning-and-organizing/01-plan-review.md`
3. `01-planning-and-organizing/02-finalise-plan.md`
4. `00-Meta-Workflow/00-meta/naming-conventions.md`

Do not edit Plan 04's debugging target, historical plans/reports, source code, or README/User Manual content in this plan; the latter are scan inputs only.

## Priority-ordered implementation roadmap

### Phase 1 — P1/S2: Resolve host metadata policy before routing outputs

**Tasks:**

1. Update `naming-conventions.md` with an explicit resolution order: (a) user-supplied output directory; (b) an explicit host policy found in the host repository's root `AGENTS.md`, root `README.md`, `User-Manual/`, root `plans/README.md`, or equivalent project documentation; (c) `00-project/` when the repository is Workflow-Scripts; (d) `project/`; (e) setup workflow if no metadata root exists (Medium).
2. State that a root-level host location may be used only when the host policy explicitly designates it; do not infer a root-level `plans/` or `research/` directory merely because it exists (Small).
3. Update the three planning workflows to use `<metadata-root>/research/` for review/research/findings outputs and `<metadata-root>/plans/` for active implementation plans; mention `<metadata-root>/build/` only as a host-documented optional location (Small each).
4. Link every routing instruction to `../00-Meta-Workflow/00-meta/naming-conventions.md` using the corrected relative hrefs from Plan 01 (Small; dependency: Plan 01's link corrections may be applied after this phase, but no concurrent writer is allowed).

**Dependency/order:** This is the first member of the coordinated planning-file batch. Plan 01 follows it; Plan 08 follows Plan 01; Plan 07 follows Plans 01, 02, and 08. Plan 04 may start only after this phase because it must mirror the selected metadata-root wording.

**Exit criteria:** Each of the three workflows resolves output locations through the policy; no workflow requires `project/` or a root-level directory when the host policy selects another root; an explicitly documented root-level host policy is honored without becoming a universal fallback.

### Phase 2 — P1/S2: Define one active-plan filename rule

**Tasks:**

1. In `naming-conventions.md`, define the default active-plan filename as `<metadata-root>/plans/YYYY-MM-DD-{plan-name}.md`, while allowing a host's explicit filename convention (Small; dependency: Phase 1 task 1).
2. Define the optional `<metadata-root>/build/` active-plan location only when the host's policy or `plans/README.md` explicitly permits it (Small; dependency: Phase 1 task 1).
3. Replace the inline dated filename template in `00-research-and-plan.md` with the policy link and use `{report-type}-YYMMDD-HHMM-{model}.md` for research/findings reports (Small; dependency: tasks 1–2).

**Exit criteria:** One policy file defines active-plan and findings filename/storage rules; the three workflows contain no competing filename template or output destination.

### Phase 3 — P2/S2: Verify downstream agreement with an explicit scan boundary

**Scan inputs:** `README.md`; `User-Manual/**/*.md`; `00-project/docs/**/*.md`; `00-Meta-Workflow/00-meta/naming-conventions.md`; and Markdown under active topical workflow directories `00-project-setup/`, `01-planning-and-organizing/`, `02-code-build/`, `03-debugging/`, `04-documentation/`, `05-review/`, `06-security/`, `07-deployment/`, `08-API-Integration/`, `10-technical-docs/`, `11-Skills/`, and `12-SEO-GEO-checklist/`.

**Exclude:** `00-Meta-Workflow/00-plans-completed/**`, `00-project/plans-completed/**`, `00-project/plans/**` (including this plan set), `00-project/research/**`, `00-project/build/**`, every `**/PLAN.reviews/**`, and backup/old-review archives. Search for output instructions, not incidental examples or historical prose.

**Tasks:**

1. Scan the exact inputs for `project/build`, `project/plans`, bare `plans/`, and conflicting research/report destinations (Small; dependency: Phases 1–2).
2. Correct only a contradictory active instruction that is within the four-file implementation scope; record any external contradiction as a separately scoped follow-up rather than expanding this plan (Small).
3. Verify `04-documentation/03-mark-completed.md:22,89-95` still discovers `<metadata-root>/plans/**` and host-optional `<metadata-root>/build/**`; verify Plan 04 still targets only `03-debugging/01-bug-description.md` and depends on this routing phase (Small).

**Exit criteria:** The scoped scan and consumer check produce a written, reproducible result with no unresolved active routing contradiction. Root `README.md`, `User-Manual/`, and `00-project/docs/` are explicitly covered rather than silently excluded.

## Risks and mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| A host has an intentional root-level or build location | Possible | Medium | Read the host policy first; honor root-level storage only when explicitly documented and build storage only when host-approved |
| Historical documents retain old paths | Likely | Low | Exclude archives and this plan set; do not rewrite historical records |
| Shared planning files are edited out of order | Possible | Medium | Enforce Plan 02 → Plan 01 → Plan 08 → Plan 07 and use one writer per file |
| A future redesign changes routing | Possible | Low | Keep this surgical policy correction; defer redesign proposals outside this plan |

## Relationship to Plan 04

Plan 04 applies the selected routing pattern to `03-debugging/01-bug-description.md`, where the output is a findings report and belongs under `<metadata-root>/research/`. Plan 02 does not edit that file. Plan 04 starts only after Phase 1, and its cross-check remains separate from this plan's four-file implementation scope.

## Acceptance criteria

- [ ] The three planning workflows contain no active conflicting `project/build/`, `project/plans/`, or bare-`plans/` output instruction.
- [ ] Metadata-root resolution first honors an explicit host root policy, then the documented Workflow-Scripts/`00-project` and host/`project` cases; it does not invent a root-level fallback.
- [ ] Research/findings output resolves to `<metadata-root>/research/` and active plans to `<metadata-root>/plans/`, using the naming policy.
- [ ] `<metadata-root>/build/` is explicitly host-optional and never presented as the universal active-plan destination.
- [ ] The active-plan and report filename rules are defined once in `naming-conventions.md` and linked by the workflows.
- [ ] The exact scan includes root `README.md`, `User-Manual/`, and `00-project/docs/`, records exclusions, and returns no unresolved active contradiction.
- [ ] `03-mark-completed.md` and Plan 04's target/dependency are verified without editing either file.

## Inline Review Addenda

### 2026-08-11 17:51 — Plan Review (Model: gpt-5.6-terra)

1. **S2/P0 — Finding:** The original plan cited a non-existent v2 proposal path and left active-workflow scan exclusions, the naming-conventions research home, Plan 04's relationship, and same-file ordering underspecified. **Evidence/rationale:** The existing naming policy defines `<metadata-root>/research/`; historical plans can contain intentionally stale wording; Plans 01/02/07 share planning files. **Actionable correction:** The v2 citation was removed, exact exclusions and policy paths were named, Plan 04's target was identified, and the coordinated order was stated. **Disposition:** Accepted and integrated.

### 2026-08-11 18:03 — Consolidated Plan Review (Model: gpt-5.6-terra)

#### P0

No additional findings.

#### P1

1. **Severity:** S2  **Priority:** P1
   **Rationale:** The current evidence demonstrates a medium-impact routing contradiction with workarounds, not a security, data-loss, or outage condition. The shared rubric maps medium impact on a likely normal path to P1; the former P0/S2 label was inflated.
   **Actionable fix:** Reclassify the plan to P1/S2 and reserve P0 for later S0/S1 evidence. Add the rubric-based gate to the plan rationale and roadmap.
   **Evidence:** `00-Meta-Workflow/00-meta/severity-priority-rubric.md:21-27,29-33,60-62`.
   **Disposition:** Accepted and integrated.

2. **Severity:** S2  **Priority:** P1
   **Rationale:** The existing metadata-root rule covers Workflow-Scripts/`00-project` and host/`project`, but does not resolve an explicit root-level host policy. A generic agent could therefore ignore a host's documented root-level routing.
   **Actionable fix:** Add the explicit host-policy-first resolution algorithm and prohibit existence-based root fallbacks. Validate it against root `AGENTS.md`, `README.md`, `User-Manual/`, and `plans/README.md` when present.
   **Evidence:** `00-Meta-Workflow/00-meta/naming-conventions.md:44-57`; current host-specific confirmation in `00-project/AGENTS.md:31-39` and `00-project/plans/README.md:1-20`.
   **Disposition:** Accepted and integrated.

#### P2

1. **Severity:** S2  **Priority:** P2
   **Rationale:** The prior downstream scan boundary named active workflow directories and `00-project/docs/agents/` but did not explicitly include the repository root README, `User-Manual/`, or the broader `00-project/docs/` compatibility/manual entry points. That ambiguity could miss a user-facing contradiction.
   **Actionable fix:** Include `README.md`, `User-Manual/**/*.md`, and `00-project/docs/**/*.md` explicitly, while retaining archive/build/review exclusions and scanning only output instructions.
   **Evidence:** Current user-facing routing appears in `README.md:13,120-137`, `User-Manual/README.md:432-446`, and `00-project/docs/user-manual.md:1-5`.
   **Disposition:** Accepted and integrated.

#### P3

No additional findings.

**Validation performed:** Read both planning workflow specifications; validated the current three planning workflows, naming policy, host metadata guidance, `03-mark-completed.md`, Plan 04, root `README.md`, `User-Manual/README.md`, and `00-project/docs/user-manual.md`; confirmed the current contradictory output instructions and the absence of a root-level host-policy rule. No source or workflow file was modified during review.
